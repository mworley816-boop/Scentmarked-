import { login, signup } from './actions'
export default async function LoginPage({searchParams}:{searchParams:Promise<{error?:string,message?:string,next?:string}>}) {
  const p=await searchParams; const next=p.next||'/collection'
  return <main><section className="auth-wrap"><p className="eyebrow">YOUR SCENTMARKED ACCOUNT</p><h2>Sign in or create an account</h2><p className="lede">Save what you own, build your wishlist, track what you've tried, and keep your favorites together in My Marks.</p>{p.error&&<div className="notice error">{p.error}</div>}{p.message&&<div className="notice">{p.message}</div>}<form className="auth-card"><input type="hidden" name="next" value={next}/><label>Display name <span>(for new accounts)</span><input name="display_name" autoComplete="name" placeholder="Your name"/></label><label>Email<input name="email" type="email" required autoComplete="email" placeholder="you@example.com"/></label><label>Password<input name="password" type="password" required minLength={6} autoComplete="current-password" placeholder="At least 6 characters"/></label><div className="button-row"><button formAction={login}>Sign In</button><button className="secondary" formAction={signup}>Create Account</button></div></form></section></main>
}
